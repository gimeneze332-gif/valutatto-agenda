rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Solo puede leer y escribir alguien que inició sesión
    // (los usuarios que crees en Authentication > Users).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
